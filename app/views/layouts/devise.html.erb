<!DOCTYPE html>
<html>
  <head>
    <title>Healthier</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
  </head>

  <body>
    
    <div class="flex justify-center p-4 border-b border-gray-300 mb-2 gap-8 text-2xl items-center font-semibold text-gray-800">
      <%= link_to 'Home', root_path, class: "cursor-pointer hover:bg-green-100 hover:text-green-500 p-3 rounded-xl" %>
      <%= link_to 'About', root_path, class: "cursor-pointer hover:bg-green-100 hover:text-green-500 p-3 rounded-xl" %>
      <div>
        <h1 class="p-3 bg-green-500 text-gray-50 font-bold rounded-full">Healther Me</h1>
      </div>
      <% unless user_signed_in? %>
        <%= link_to 'Login', new_user_session_path, class: "cursor-pointer hover:bg-green-100 hover:text-green-500 p-3 rounded-xl" %>
        <%= link_to 'Signup', new_user_registration_path, class: "cursor-pointer hover:bg-green-100 hover:text-green-500 p-3 rounded-xl" %>
      <% end %>
    </div>
    
    <div>
      <%= notice %>
      <%= alert %>
    </div>
    
    <%= yield %>
    
  </body>
</html>
